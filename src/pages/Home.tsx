import { BoardCard } from "@/components/BoardCard";
import { BoardCardSkeleton } from "@/components/BoardCardSkeleton";
import { ErrorMessage } from "@/components/ErrorMessage";
import { HorizontalBoardCard } from "@/components/HorizontalBoardCard";
import { Layout } from "@/components/Layout";
import { LoadingSpinner } => (                 <ErrorMessage error={resolvedError} onRetry={() => refetch()} />               ) : boards && boards.length > 0 ? (                 boards.map((board) => (                   <HorizontalBoardCard                     key={board.id}                     board={board}                     onShare={(boardId) => {                       const targetBoard = boards.find((b) => b.id === boardId);                       if (targetBoard) {                         setShareDialogBoard({                           id: targetBoard.id,                           name: targetBoard.name,                           shareToken: targetBoard.share_token                         });                       }                     }}                   />                 ))               ) : (                 <p className="text-neutral-500">No boards yet. Create one!</p>               )}             </div>             {/* Right column: Waterfall Showcase */}             <div className="w-2/3">               <ShowcaseBoard userId={user.id} />             </div>           </section>         </Layout>       );     }   
